---
description: "에러 래핑·sentinel 지양·패닉 최소화·구조적 로깅"
globs:
  - "**/*.go"
---

# ERRORS & PANICS

- 에러는 `%w`로 래핑하고, 호출 경로에서 의미를 보강한다.
- 비교가 필요한 에러는 `errors.Is/As` 패턴 사용. 별도 sentinel 남발 금지.
- 라이브러리 내부 `panic` 지양, 진입점(main/server)에서만 복구.
- 로그는 구조화(키는 snake_case). 에러 로그에는 PII/시크릿 금지.